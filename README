Для сборки используется qbs

Для запуска передать следующие аргументы: 1. количество вводимых чисел (обязательно) и количество потоков, создаваемых для расчета нок-ов (необязательно)

К сожалению, на убунте 12.04 получилось собрать только с помощью g++ (на openSUSE 13.2 можно clang++)
